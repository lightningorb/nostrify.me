// can't import this... so will inline it. carefully when updating.
export interface Profile {
	name: string;
	website: string;
	npub: string;
	picture: string;
	about: string;
	nip05: string;
	nip05valid: boolean;
}
